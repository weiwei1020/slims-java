<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.lims.mapper.CompanyMapper">
    <resultMap id="Company" type="com.example.demo.lims.vo.Company">
        <id property="companyId" column="company_id"/>
        <result property="companyName" column="company_name"/>
    </resultMap>
    <insert id="insertCompany" parameterType="company">
        insert  into company (company_name) values (#{companyName})
    </insert>

    <delete id="deleteCompany" parameterType="company">
        delete from company
        where company_id= #{companyId}
    </delete>

    <update id="updateCompany" parameterType="company" >
        UPDATE company
        SET
        company_name  = #{companyName}
        WHERE company_id = #{companyId}
    </update>

    <select id="selectCompany" resultMap="Company">
        select * from company
        where company_id= #{companyId}
    </select>

    <select id="selectAllCompany" resultMap="Company">
        select * from company
    </select>

    <select id="selectCount" resultType="int">
        select count(*) from company
    </select>
<!--limit #{start},#{size}-->
    <select id="pageCompany" parameterType="Map" resultMap="Company">
        select *
        from Company where company_name LIKE concat('%',#{companyName},'%')
            limit #{start},#{size}
    </select>

</mapper>
